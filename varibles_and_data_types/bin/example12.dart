void main(List<String> args) {
  //type promotion go from more precise type to lesser but not the other way around
  final double hisAge = 30;
  print(hisAge);
  //not allowed
  // final int herAge = 40.5;
}
