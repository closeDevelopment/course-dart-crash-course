void main(List<String> args) {
  const theirNames = ['Alice', 'Bob', 'Carol'];
  print(theirNames);
  // this will be pick up by linter if rule is applied
  // final Wow = 'wow';
  // print(wow);
  const SomeName = 'Don';
  print(SomeName);
}
