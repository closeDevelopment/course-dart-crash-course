void main(List<String> args) {
  // ignore: unused_local_variable
  final age = 10;

  // constants cannot be assigned to a non constant value
  // so this would be invalid operation
  //const age2 = age;
}
